NEXT_PUBLIC_API_URL=http://localhost:3001/api
# Socket origin; defaults to the API origin when unset
NEXT_PUBLIC_SOCKET_URL=
# Must match the API's REFRESH_TOKEN_TTL_DAYS (defaults to 7)
NEXT_PUBLIC_REFRESH_TTL_DAYS=7
